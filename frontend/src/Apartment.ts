export type Apartment = {
  id: number;
  title: string;
  price: number;
  rooms: number;
  floor: number;
  floor_area: number;
  tower: string;
  orientation: string;
  image?: string;
};

// definisanje podataka iz backend-a frontend-u
